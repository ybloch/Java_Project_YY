<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.java_4033_7272">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="com.JCT.AppDataBase"
            android:name=".model.backend.AppContentProvider"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".controller.ActivitytListActivity"/>
        <activity android:name=".controller.BusinessListActivity"/>
        <activity android:name=".controller.LogInActivity"/>

    </application>

</manifest>